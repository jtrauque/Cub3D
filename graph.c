#include "cub3d.h"

/*
void  ft_detect(t_pars pars, t_params params)
{
  float x;
  float y;
  float step;
  int i;
  t_obs obs;

  x = fasbf(pars.px - pars.dx);
  y = fasbf(pars.py - pars.dy);
  if(x >= y)
    step = x;
  else 
    step = y;
  x = x /step;
  y = y / step;
  i = 1;
  while (i <= step)
  {
    pars.px = pars.px + x;
    pars.py = pars.py + y;
    i++;
  }
  obs.ox = pars.px;
  obs.oy = pars.py;
}*/
